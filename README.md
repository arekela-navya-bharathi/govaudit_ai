commands to run :
go to project folder  cd <foder-name>
then do npm install
next lets actiavte python environment 
open command line : serach cmd in your laptop search
1.python -m  venv .venv 
2. .venv\Scripts\actiavte
3. pip -r install -r requirements.txt

do it in two cmd as we need to run frontend and backend

for frontend : npm run dev
for backend :uvicorn main:app --reload  

note : install python
